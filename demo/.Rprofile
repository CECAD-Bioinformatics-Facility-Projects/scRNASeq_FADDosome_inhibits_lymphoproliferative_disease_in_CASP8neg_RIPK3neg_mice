# Activate the project renv library so packages are available when knitting
# from the demo/ subdirectory (renv/activate.R lives one level up).
source(file.path(dirname(getwd()), "renv", "activate.R"))
